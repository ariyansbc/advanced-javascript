
let bonus = 50; // global variable
function sum(first ,sec) {
    let result = first + sec + bonus;
    // console.log("result:",result);
    if(result > 50) {
        const mood = "Happy";
        console.log(mood);
    }
    return result;
}
 const output = sum(6,7);
  
  console.log(day);
  let day = "friday";

//  console.log(result);
//  console.log("output with bonus:",output);
